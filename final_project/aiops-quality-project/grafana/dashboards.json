{
  "title": "AIOps Quality Dashboard",
  "panels": [
    {"type": "graph", "title": "Requests per minute", "targets": [{"expr": "rate(api_requests_total[1m])"}]},
    {"type": "graph", "title": "Latency", "targets": [{"expr": "histogram_quantile(0.95, sum(rate(api_latency_seconds_bucket[5m])) by (le))"}]}
  ]
}
